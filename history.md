# SpanWM 개발 히스토리 (v0 → v7)

한 줄 목표: **워터마크를 텍스트 전체가 아니라, 파서로 다시 찾을 수 있는 syntactic span 하나에만 심고 그 부분만 검출한다.**

## 한눈에 보기

| 버전 | 핵심 방식 | 모델 | AUROC | 결말 |
|---|---|---|---|---|
| v0 | 설계 + 파서 탐색 (코드 골격) | — | — | 계약·설계 확정 |
| v1 | left-AR, span 길이만큼 재생성 | Qwen instruct | **0.55** | 재구성 extent 문제 |
| v2 | chat infilling (빈칸 채우기) | Qwen instruct | **0.47** | 엔트로피 + 토큰화 drift |
| **v3** | **base + left-AR + 고정 K=20** | **Llama base** | **0.997** ✅ | **성공** |
| v4 | base + few-shot infilling | Llama base | **0.86** | v2보다↑, v3엔 못 미침 |
| v5 | v3 + per-span PRF 역할 선택 | Llama base | **0.986** | 역할 다양화, 소폭 대가 |
| v6 | v5 + multi-span (6토큰×4곳 합산) | Llama base | **0.980** | 손상 분산·내결함성, 검출력 소폭↓ |
| v7 | v6 + splice 수정 (경계 drift·지문 제거) | Llama base | **0.989** | v5 넘어섬 + multi-span 장점 유지 |

γ(green list 비율)와 판정법도 같이 진화: v1·v2초반 γ0.5 + z-score → v2후반부터 **γ0.25 + exact binomial p**.

---

## v0 — 설계와 파서 탐색

- **한 것**: `README.md`에 아이디어 정리, spaCy + textacy로 문장을 파싱해보며 어떤 span을 쓸지 탐색(`parser_testing/`). MarkLLM 골격 위에 코드 계약 확립.
- **정한 것들** (이후 계속 유지):
  - span 선택은 **고정 역할(S/V/O 중 하나)** 을 key로 선택 (candidate 목록 인덱스 ❌ — 재생성하면 목록이 흔들려서).
  - textacy raw S/V/O는 1~2토큰이라 너무 짧음 → **noun_chunk / 동사구로 확장**.
  - char offset ↔ model 토큰 매핑은 **trim-then-overlap** (BPE 앞공백 토큰이 안 잘리게).
- **아직 없음**: 실제로 검출되는 워터마크.

## v1 — 첫 end-to-end (left-AR)

- **방식**: draft 생성 → 역할 span 선택 → 그 자리를 **왼쪽 문맥만 주고 span 길이만큼 재생성**(워터마크 on) → splice. 검출은 재파싱해서 그 constituent를 z-test. γ0.5.
- **결과**: AUROC **0.55** (거의 랜덤).
- **왜 실패**: 재생성이 문장 구조를 바꿔서, 검출이 embed보다 **짧은 sub-span**을 복원함. 시작점은 84% 맞지만 끝이 줄어들어 검사 토큰 N이 폭락 → z가 threshold를 못 넘음.
- **교훈**: 파서의 "constituent 끝"은 재생성에 불안정하다.

## v2 — chat infilling

- **방식**: `"왼쪽 [빈칸] 오른쪽, 빈칸을 채워라"`를 instruct 모델에 시켜 그 부분만 재생성. (진행 중 γ0.25 + exact-p 도입.)
- **결과**: AUROC **0.47** (랜덤보다 나쁨).
- **왜 실패 (2가지, 생성 시점 추적으로 확인)**:
  1. **저엔트로피**: 빈칸을 좌우로 꽉 조이면 답이 거의 확정됨(`Middletown`) → 워터마크가 밀 여지가 없어 생성 시점 green율이 0~29%.
  2. **토큰화 drift**: 생성 땐 `"performance"`인데 문장에 끼우면 `" performance"`(앞공백) → BPE id가 달라져 검출이 **다른 토큰**을 검사.
- **교훈**: **재구성 가능한(짧고 확정적인) span = 저엔트로피 = 워터마크가 안 실린다.** infilling은 이 함정에 정면으로 빠짐.

## v3 — base + left-AR + 고정 K=20 (성공)

- **방식**: **base 모델**(Llama-3.2-3B)로 **왼쪽 문맥만 주고 자유생성**(고엔트로피 → 워터마크 실림 + 순수 좌→우라 토큰화 정합). **앵커에서 고정 20토큰**을 워터마크·검사(불안정한 constituent 끝을 안 씀 → N=20 보장). γ0.25 + exact binomial p. 재생성 후 **앵커가 그대로 복원되는지 검증**하고 안 되면 재시도.
- **결과**: AUROC **0.997**, mean z(pos) +5.83 vs (neg) −0.05, **TPR@FPR=0.1%도 0.97**, 검증률 92%.
- **왜 성공**: v1(재구성 extent)·v2(엔트로피+drift)를 한 번에 해결 — 자유생성으로 엔트로피 회복, 고정 K로 N 확보, base 좌→우로 토큰화 일치.
- **대가**: K=20 자유생성이 짧은 span을 덮고 넘쳐서 **워터마크된 부분 텍스트가 의미적으로 튐** (검출력은 확보, 품질은 다음 과제).

## v4 — base + few-shot infilling

- **동기**: base 모델로도 infilling을 시켜보자 (base는 zero-shot 지시는 못 따르지만 **few-shot 예시**를 주면 패턴을 따라함).
- **방식**: base Llama에 `"Passage ... [BLANK] ... / Answer: ..."` 예시 몇 개를 준 뒤 우리 빈칸을 채우게 함. 검출은 v2식 constituent. γ0.25 + exact-p.
- **결과**: AUROC **0.86**, mean z(pos) +1.63, TPR@FPR=0.1% 0.075, 검증률 96%.
- **해석**: v2(0.47)보다 크게 나아짐 — base라서 chat 스캐폴드 **토큰화 drift가 사라진** 덕. 하지만 v3(0.997)엔 한참 못 미침 — **fill이 짧아(N~2~5)** 토큰당 신호가 약하고, 엄격한 FPR에서 무너짐.
- **교훈**: 갈림은 "infilling ↔ left-AR"이 아니라 **"N이 짧냐 기냐"**. v3의 고정 K=20이 N을 확보한 게 결정적이었음.

## v5 — v3 + per-span PRF 역할 선택

- **동기**: v3는 master_key가 역할 하나를 영구 고정(전부 OBJECT) → 공격자가 역할을 알면 취약. 위치마다 역할이 달라지되 key로 재현 가능하게.
- **방식**: span 후보를 순서대로 스캔, `target = PRF(master_key ‖ span 앞 16자) % 3` 이 지정하는 역할과 실제 역할이 일치하는 첫 span 채택. n-gram은 **anchor 앞(left)** 문자에서 뽑음(left-AR이 안 건드리는 부분이라 embed·detect에서 동일). 나머지는 v3 그대로.
- **결과**: AUROC **0.986**, mean z(pos) +5.34, TPR@FPR=0.1% 0.89~0.92. 역할 분포 O112/P44/S44 (실제로 갈림). 검증률 68% (v3 92%보다↓).
- **해석**: 역할 다양화의 대가는 소폭(AUROC −0.011). 검증률이 내려간 건 재생성 후에도 "그 위치의 PRF 역할 = 실제 역할"이 다시 성립해야 해서 조건이 더 까다롭기 때문. 단 K=20 window가 앵커가 약간 밀려도 워터마크 영역을 덮어서 unverified도 상당수 검출됨.

## v6 — multi-span (작은 window 여러 개 합산)

- **동기**: "multi-span이면 큰 K=20이 필요 없지 않나" — 한 곳 20토큰 rambling 대신 여러 곳에 작게 나눠 심자.
- **방식**: embed·detect가 같은 좌→우 스캔 공유. 사이트마다 **6토큰** window 재생성(v5 PRF 역할 규칙), 최대 4곳 → 총 N=24. 검출은 4개 window 토큰을 **합산해 exact binomial 한 번**. 작은 고정 window 자체는 유지(constituent 끝 불안정은 여전하므로) — 억지의 크기가 20→6으로 준 것.
- **결과**: AUROC **0.980**, mean z +4.72, TPR@FPR=0.1% 0.74~0.79. 4곳 전부 정렬은 25%.
- **해석**: 검출력은 v5보다 소폭↓ — 부분 정렬 실패(1~2곳)가 pooled 통계를 **희석**. 대신 품질 손상이 분산·축소되고(6토큰×4곳), 사이트 하나 실패해도 검출되는 **내결함성** 획득. 검출력↔품질↔견고성의 삼자 트레이드오프가 명시적으로 드러남.

## v7 — splice 수정 (경계 drift + 지문 제거)

- **동기 (v6 검진에서 발견한 두 결함)**:
  1. **anchor 경계 재토큰화 drift**: `left = text[:anchor]`가 거의 항상 span 앞 공백으로 끝남 → 생성 시엔 그 공백이 **단독 토큰(220)**, splice 후 재토큰화에선 fill 첫 단어와 merge(`" dog"`) 되거나 fill의 앞공백과 겹쳐 이중공백이 됨. 위치별 green율 실측(v6 n200, GPU)이 **pos0 0.75 → pos5 0.53으로 하락**하는 프로파일을 보임 — 주 손실은 "첫 토큰"이 아니라 **재토큰화로 fill 토큰 수가 줄면서 window 꼬리가 워터마크 없는 원문으로 밀려나는 것**. 전체 green율 0.627 (v5 ~0.78 대비 z 5.34→4.72 하락을 설명).
  2. **splice 지문**: `sep_r`이 구두점 앞에도 공백을 삽입 → `"Bharti ,"` 아티팩트가 **워터마크 133/200 vs 비워터마크 1/200** — 정규식 하나로 구분되는 스텔스 붕괴. 이중공백 지문도 23/200 vs 0/200.
- **방식** (`watermark/spanwm_v7/`, v6 상속·검출기는 v6와 동일):
  - **공백 뗀 문맥으로 생성**: left 끝 공백을 떼고 생성시켜 모델이 leading-space 토큰을 직접 내게 함 → splice 결과가 생성 토큰 그대로 재토큰화되어 greenlist seed까지 일치. fill이 공백으로 안 시작하면 공백 복원 fallback.
  - **구두점 부착 규칙**: `,.;:!?)]}…` 등 앞엔 sep 삽입 안 함; fill 꼬리 공백은 right가 경계를 제공하면 제거; 구두점으로 시작하는 fill은 3회 재샘플, 그래도면 공백 없이 직접 부착(그 사이트는 희생 — 어차피 파스가 어긋나는 자리).
- **결과 (n200, neg=unwatermarked)**: AUROC **0.9891**, mean z **+5.45** (neg −0.04), TPR@FPR=10/5/1/0.1% = 0.980/0.973/0.956/**0.892**, verified 74/200 (v6 25%→37%). **v5(0.986/+5.34)를 넘으면서** multi-span의 품질 분산·내결함성은 유지 — v6에서 잃었던 검출력이 splice 수정만으로 전부 회복되고도 남음. smoke 단계 확인: 검출 green율 0.63→0.82(위치별 평탄, 하락 프로파일 소멸), 구두점·이중공백 지문 0건.
- **교훈**: splice 경계는 "문자열이 자연스러운가"가 아니라 **"재토큰화가 생성 토큰열을 그대로 복원하는가"**로 설계해야 한다. multi-span은 경계 수를 4배로 늘리므로 경계당 고정 손실이 그대로 4배가 된다.
- **추가 발견 (v7 작업 중, 전 버전 공통)**: `torch.randperm`은 같은 seed라도 **CPU/CUDA generator가 다른 수열**을 냄 → embed(GPU)와 다른 device에서 detect하면 greenlist가 달라져 **조용히 AUROC 0.5**가 됨 (CPU 스캔 실측: verified 샘플조차 green율 ≈ γ=0.25). detect는 반드시 embed와 같은 device 종류에서 실행.

---

## 관통하는 교훈

> **검출력의 열쇠는 "엔트로피"와 "충분한 N"이다.**
> 재구성하기 쉬운 짧은 syntactic span은 저엔트로피라 워터마크가 안 실린다.
> v3는 자유생성(엔트로피)+고정 K(N)+base(토큰화 정합)로 이를 우회했다.

세부 구현·실행법은 `CLAUDE.md` 참고.
